# Chunk dt-1: Make all DECtalk voices selectable declaratively

Datestamp: 2026-05-29

## Mission
All 10 DECtalk voices (paul, harry, frank, dennis, betty, ursula, wendy, rita, kit, chris)
selectable in `dectalk-english` frontend, fully declaratively. Today only Paul exists inline
in `public/rules/frontends/dectalk-english/frontend.yaml` (~lines 275-311, parameters.policy.speaker).
Source data already converted: `C:\Users\Q\src\dectalk\463\output\qlatt\speakers\*.yaml`.

## Scope
- IN: declarative storage of 10 voices; name-keyed selection; map selected voice into fields
  pipeline ALREADY consumes (policy.speaker + F0 f0_minimum/f0_scale_factor/f0_lp_filter + hat/fall/assertiveness).
  Paul = DEFAULT, byte-identical to today.
- OUT (defer): wiring F4-F8 / per-voice gains / glottal into synth graph & semantics. STORE in YAML now.

## Baseline (2026-05-29 09:xx)
- branch: dectalk-parity (correct). Tracked files clean; only untracked notes/scripts present.
- `npx vitest run`: **1105 passed / 125 files** — GREEN, matches expected baseline.
- `npm run test:golden`: **EXIT=1**. INVESTIGATING — need to determine if this is a real
  failure or a known/pre-existing nonzero exit (deprecation warnings / tolerance reporting).
  Did NOT change anything yet, so exit=1 is the PRE-CHANGE state.

## Golden exit=1 CLASSIFIED — pre-existing, out of scope
`test:golden` runs 3 sub-scripts. Results:
- klatt-tract-wasm-compare.ts EXIT=0
- lf-source-wasm-compare.ts **EXIT=1**  <-- the failure
- render-phrase.ts EXIT=0
lf-source compare loads `public/worklets/lf-source.wasm` and compares vs golden
`test/golden/klatt_paper.json` lfLm.samples; requires maxDelta<=1e-5, observes maxDelta=0.79,
rmsError=0.325. Both artifacts are committed/clean at HEAD fa84c772 (git status empty) -> this
failure is PRE-EXISTING on the untouched branch, independent of my work.
Likely cause: LF-source golden reference stale vs recent LF-source commits (fa84c772 "Add DI
param", a5fa1c21 "fix jitter formula"). Proposed fix (OUT of dt-1 scope, DSP golden data):
regenerate lfLm.samples in klatt_paper.json from current lf-source.wasm.

DECISION: vitest (1105/1105) is fully GREEN and exercises the speaker/frontend pipeline I touch.
My changes are ZERO TS-DSP / ZERO WASM, so they cannot affect this golden compare. Proceed with
the chunk; verify golden does not REGRESS (lf-source still the only failure). Hard-stop (b) reading:
the green baseline that matters for this chunk (vitest + the non-LF golden scripts) reproduces.

## Plumbing observed
- dectalk-english frontend.yaml declares NO speaker_profile_path -> resolveSpeakerProfile uses
  the SHARED generic /rules/policy/speaker-profile.yaml (base_f0=110). The inline Paul block at
  parameters.policy.speaker (lines 275-311) feeds ONLY speakerParams (the F0 layered model),
  not resolveSpeakerProfile.
- speakerParams builder (tts-frontend.ts ~528-544): iterates ALL keys of parameters.policy.speaker
  via readPolicyNumber, then OVERWRITES base_f0_hz with resolvedSpeaker.base_f0_hz.
- Fields the pipeline ACTUALLY consumes from policy.speaker (12): base_f0_hz, formant_scale,
  rd_default, spectral_tilt_offset_db, f0_minimum, f0_scale_factor, f0_lp_filter,
  f0_lp_filter_alpha, hat_rise_hz10, scale_str_rise, assertiveness, baseline_fall_hz10.
  (Inline Paul has f0_lp_filter_alpha=0.5126953125=2100/4096; lacks falling_target/F4-F8/gains.)
- options.speaker is currently a partial numeric override OBJECT (not a name). Two qlatt-english
  tests pass the object form -> must keep working.
- loadYamlDocumentSync loads /rules/... via FS(node)+XHR(browser); /rules/frontends/.../speakers/x.yaml
  loads the same way. No Vite glob needed.

## Design CHOSEN
- Storage: public/rules/frontends/dectalk-english/speakers/<name>.yaml x10 (generated by
  scripts/dt1-import-voices.ts from converted source; adds derived f0_lp_filter_alpha + citations). DONE.
- Registry: frontend.yaml `speakers: {dir, default: paul, voices:[...]}`. DONE.
- Selector: src/dectalk-voice.ts (generic, zero voice names) getVoiceRegistry + resolveVoice. DONE.
- Wiring (in progress): widen options.speaker to (SpeakerProfileOverride | string). If string ->
  resolveVoice -> override feeds resolveSpeakerProfile; voice.params merged OVER policy.speaker for
  speakerParams. Object form unchanged. Add resolvedSpeaker+speakerParams to detailed result for probe.
- Paul default byte-identical: when speaker absent/"paul", policy.speaker inline block untouched and
  resolvedSpeaker uses generic profile exactly as today. Selecting paul explicitly should reproduce
  the same consumed values (paul.yaml == inline block consumed fields). VERIFY via probe.

## Deferred (per scope, store-only)
F4-F8, gains (GF/GH/GV/GN/G1-G4/LO), glottal (AGO/AGVO/AGUO/UNVOW/CHINK, smoothness/breathiness/
richness/nopen_fraction/laryngealization/head_size/quickness), falling_target — stored in voice
YAML, NOT wired to audio. Later chunk.

## EVIDENCE so far
- Probe (scripts/dt1-voice-probe.ts) paul vs betty:
  paul -> base_f0=122 formant_scale=1.0 f0_minimum=1100 f0_scale_factor=4100 f0_lp_filter=2100
  betty-> base_f0=208 formant_scale=1.17 f0_minimum=1960 f0_scale_factor=4920 f0_lp_filter=2175
  DIFFER as required; betty AP 208 > paul AP 122. DoD#2 MET.
- IMPORTANT QUIRK observed: dectalk DEFAULT (no speaker option) resolves base_f0=110 via the GENERIC
  /rules/policy/speaker-profile.yaml, NOT Paul's 122. This is PRE-EXISTING: the inline Paul
  base_f0:122 was always shadowed by resolvedSpeaker(110) because dectalk declares no
  speaker_profile_path. My change keeps the default byte-identical (default_speakerParams unchanged;
  only difference vs explicit "paul" is base_f0 110->122, propagated into frame params/F0). I did NOT
  "fix" the default to 122 because that would break the byte-identical-default hard requirement.
  Selecting speaker:"paul" explicitly gives Paul's true 122. Documented, not changed.
- vitest AFTER changes: 1105 passed / 125 files. Default dectalk path unchanged. DoD#3 (vitest) MET.

## Files changed/added
- ADD public/rules/frontends/dectalk-english/speakers/{paul,harry,frank,dennis,betty,ursula,wendy,rita,kit,chris}.yaml
- ADD src/dectalk-voice.ts (generic selector: getVoiceRegistry, resolveVoice)
- ADD scripts/dt1-import-voices.ts (importer), scripts/dt1-voice-probe.ts (probe)
- EDIT public/rules/frontends/dectalk-english/frontend.yaml (speakers registry block)
- EDIT src/tts-frontend.ts (widen speaker option to string|object; resolve voice; merge into
  speakerParams; expose resolvedSpeaker+speakerParams on detailed result)

## FINAL EVIDENCE (all DoD met)
- DoD#1 (10 voices as YAML): 10 files in public/rules/frontends/dectalk-english/speakers/. All load;
  all carry DECtalk 4.63 ph_vset.c + Ph_drwt02.c citations.
- DoD#2 (name selection populates policy): probe paul vs betty differ (122 vs 208 base_f0;
  1100 vs 1960 f0_minimum; 1.0 vs 1.17 formant_scale). All 10 probed, distinct & matching source.
- DoD#3 (Paul default unchanged): vitest 1105 passed / 125 files (== baseline). golden EXIT=1 ==
  baseline (only lf-source-wasm-compare fails, PRE-EXISTING; klatt-tract + render-phrase pass).
  No regression. Default dectalk path byte-identical (default_speakerParams unchanged).
- DoD#4 (no imperative per-voice code): grep of src/dectalk-voice.ts + src/tts-frontend.ts for
  voice-name conditionals -> NONE. Only voice names in TS are JSDoc examples ("paul","betty").
  Selector is purely generic (registry -> file by name).
- DoD#5 (citations preserved): every voice YAML has file-level citations; f0_lp_filter_alpha
  derivation cited (Ph_drwt02.c frac4mul /4096).
- Typecheck: src/tts-frontend.ts + src/dectalk-voice.ts clean. 2 pre-existing src/ TS errors
  (load-experiment-config.ts, wasm-utils.ts) are in committed untouched files, not mine.
- Unknown voice -> E_VOICE_UNKNOWN with available-voice list (fails loud, no silent fallback).

## KEY DECISION / quirk to flag to orchestrator
dectalk DEFAULT base_f0 is 110 (generic /rules/policy/speaker-profile.yaml), NOT Paul's 122.
PRE-EXISTING: inline Paul base_f0:122 was always shadowed because dectalk declares no
speaker_profile_path. I kept the default byte-identical (hard requirement) rather than "fixing"
it to 122. Selecting speaker:"paul" explicitly yields Paul's true 122. If the orchestrator wants
the dectalk default to actually BE Paul (122), that is a follow-up: either set
speaker_profile_path to a Paul profile, or default the registry resolution to run even when no
speaker option is passed. NOT done here to preserve byte-identity.

## DEFERRED (stored in voice YAML, not wired to audio — per scope)
F4-F8/B4/B5, gains GF/GH/GV/GN/G1-G4/LO, glottal AGO/AGVO/AGUO/UNVOW/CHINK,
smoothness/breathiness/richness/nopen_fraction/laryngealization/head_size/quickness, falling_target.
