[package]
name = "noise-source"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
klatt-wasm-common = { path = "../klatt-wasm-common" }

[dev-dependencies]
impulse-train = { path = "../impulse-train" }
differentiator = { path = "../differentiator" }
chalker-radiation = { path = "../chalker-radiation" }
glottal-mod = { path = "../glottal-mod" }
